interface:
  display_name: "Agent Development Loop"
  short_description: "Run durable phased implementation and review"
  default_prompt: "Use $agent-loop to run a committed Coordinator, Implementer, and Reviewer loop until every phase is approved."
