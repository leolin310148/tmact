interface:
  display_name: "tmact Dispatch"
  short_description: "Run background work in another tmux session"
  default_prompt: "Use $tmact-dispatch to run this task in another tmux session without waiting for an explicit reply."
